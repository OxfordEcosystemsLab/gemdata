<%= form_for(@batch) do |f| %>
  <% if @batch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@batch.errors.count, "error") %> prohibited this batch from being saved:</h2>

      <ul>
      <% @batch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :import_address %><br>
    <%= f.text_field :import_address %>
  </div>
  <div class="field">
    <%= f.label :started %><br>
    <%= f.datetime_select :started %>
  </div>
  <div class="field">
    <%= f.label :finished %><br>
    <%= f.datetime_select :finished %>
  </div>
  <div class="field">
    <%= f.label :transaction_passed %><br>
    <%= f.datetime_select :transaction_passed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
